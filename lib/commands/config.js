const fs = require("fs");
const path = require("path");
const LocalStorage = require("./../util/localstorage.js")

const pkg = JSON.parse(fs.readFileSync(path.resolve(__dirname, "./../../package.json"), "utf8"));

const conf = new LocalStorage("mmm", { repo: "https://raw.githubusercontent.com/Magisk-Modules-Alt-Repo/json/main/modules.json" })

async function config() {
switch (opt) {
    case value:
        
        break;

    default:
        break;
}
}



module.exports = { rootfs, configPath }
